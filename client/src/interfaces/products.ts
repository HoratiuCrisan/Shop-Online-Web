export default interface Product {
    id: number;
    name: string;
    category: string;
    quantity: number;
    description: string;
    photoUrl: string;
    price: number;
    discount: number;
}